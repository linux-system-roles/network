# SPDX-License-Identifier: BSD-3-Clause
---
- include_tasks: get_interface_stat.yml  # noqa name
- name: "Assert that interface {{ interface }} is present"  # noqa name
  assert:
    that: interface_stat.stat.exists
    msg: "{{ interface }} does not exist"
